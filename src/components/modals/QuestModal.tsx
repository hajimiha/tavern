import { npcs, shopItems, crops, itemDisplayNames } from '../../game/data'
import { useGame } from '../../game/GameContext'

export function QuestModal({ journal = false }: { journal?: boolean }) {
  const { state, dispatch } = useGame()
  return <div className="quest-modal-content"><div className="quest-board-intro"><span>{journal ? '个人任务手册' : '壁炉旁的黄铜委托板'}</span><p>委托完成后，发布者与村长洛岚的好感会同时提升。任务物品在进行中受到出售保护。</p></div><div className="quest-card-list">{state.quests.map((quest) => { const issuer = npcs.find((npc) => npc.id === quest.issuerId)!; const itemName = shopItems.find((item) => item.id === quest.requiredItemId)?.name ?? crops.find((crop) => crop.id === quest.requiredItemId)?.name ?? itemDisplayNames[quest.requiredItemId] ?? '未鉴定物品'; const held = state.inventory[quest.requiredItemId] ?? 0; return <article id={`quest-card-${quest.id}`} key={quest.id} className={`quest-card status-${quest.status}`}><header><div><span>{issuer.role} · {issuer.name}</span><h3>{quest.title}</h3></div><em>{quest.expiresInDays}日内</em></header><p>{quest.description}</p><div className="quest-progress"><span style={{ width: `${Math.min(100, held / quest.requiredAmount * 100)}%` }} /><small>{itemName} {held} / {quest.requiredAmount}</small></div><footer><span>{quest.rewardMoney} 金币 · 发布者 +{quest.rewardAffinity} · 村长 +{quest.mayorAffinity}</span>{quest.status === 'available' ? <button id={`quest-accept-${quest.id}`} type="button" onClick={() => dispatch({ type: 'ACCEPT_QUEST', questId: quest.id })}>接受委托</button> : <strong>{quest.status === 'completed' ? '已经完成' : held >= quest.requiredAmount ? '可向发布者交付' : '进行中'}</strong>}</footer></article> })}</div></div>
}
